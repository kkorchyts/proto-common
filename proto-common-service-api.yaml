apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: proto-common-service
  description: gRPC API for Proto common validation
  owner: team-a
spec:
  type: grpc
  lifecycle: experimental
  definition:
    $text: https://github.com/kkorchyts/proto-common/blob/main/src/main/proto/user.proto
    
